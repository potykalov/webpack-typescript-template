/**
 * Подробное описание каждого параметра конфигурации:
 * https://jestjs.io/docs/configuration
 */

/** @type {import('jest').Config} */
const config = {
  // Автоматически создавать моки для всех импортируемых модулей в тестах
  // automock: false,

  // Остановить выполнение тестов после `n` ошибок
  // bail: 0,

  // Каталог, в котором Jest должен хранить кэш информации о зависимостях
  // cacheDirectory: "/private/var/folders/s5/sjl4h5t129ndfngc5vgykl240000gn/T/jest_dx",

  // Автоматически очищать вызовы, экземпляры, контексты и результаты моков перед каждым тестом
  clearMocks: true,

  // Собирать ли информацию о покрытии кода во время выполнения тестов
  collectCoverage: false,

  // Массив glob-шаблонов файлов, для которых необходимо собирать покрытие
  collectCoverageFrom: ["src/**/*.js", "!src/**/__tests__/**"],

  // Каталог, в который Jest должен сохранять отчёты о покрытии
  coverageDirectory: "coverage",

  // Массив регулярных выражений для файлов, которые нужно исключить из покрытия
  // coveragePathIgnorePatterns: [
  //   "/node_modules/"
  // ],

  // Провайдер, используемый для сбора информации о покрытии
  // coverageProvider: "babel",

  // Форматы отчётов о покрытии
  // coverageReporters: [
  //   "json",
  //   "text",
  //   "lcov",
  //   "clover"
  // ],

  // Минимальные требования к покрытию кода
  coverageThreshold: {
    global: {
      lines: 100,
    },
  },

  // Путь к пользовательскому модулю для извлечения зависимостей
  // dependencyExtractor: undefined,

  // Выбрасывать ошибки при использовании устаревших API
  // errorOnDeprecated: false,

  // Настройки таймеров-заглушек
  // fakeTimers: {
  //   "enableGlobally": false
  // },

  // Принудительно собирать покрытие для указанных файлов
  // forceCoverageMatch: [],

  // Путь к модулю с асинхронной функцией, выполняемой один раз перед запуском всех тестов
  // globalSetup: undefined,

  // Путь к модулю с асинхронной функцией, выполняемой один раз после завершения всех тестов
  // globalTeardown: undefined,

  // Глобальные переменные, доступные во всех тестовых окружениях
  // globals: {},

  // Максимальное количество процессов для выполнения тестов
  // Можно указать процент или число, например: "50%" или 2
  // maxWorkers: "50%",

  // Каталоги, в которых Jest будет искать модули
  // moduleDirectories: [
  //   "node_modules"
  // ],

  // Расширения файлов, используемые модулями проекта
  // moduleFileExtensions: [
  //   "js",
  //   "mjs",
  //   "cjs",
  //   "jsx",
  //   "ts",
  //   "mts",
  //   "cts",
  //   "tsx",
  //   "json",
  //   "node"
  // ],

  // Сопоставление регулярных выражений с именами или путями модулей
  // moduleNameMapper: {},

  // Пути к модулям, которые Jest должен игнорировать
  // modulePathIgnorePatterns: [],

  // Показывать системные уведомления о результатах тестирования
  // notify: false,

  // Режим отображения уведомлений. Требует `notify: true`
  // notifyMode: "failure-change",

  // Предустановленная конфигурация Jest
  // preset: undefined,

  // Запуск тестов из одного или нескольких проектов
  // projects: undefined,

  // Пользовательские репортёры Jest
  // reporters: undefined,

  // Автоматически сбрасывать состояние моков перед каждым тестом
  // resetMocks: false,

  // Сбрасывать реестр модулей перед каждым отдельным тестом
  // resetModules: false,

  // Путь к пользовательскому резолверу модулей
  // resolver: undefined,

  // Автоматически восстанавливать состояние и реализацию моков перед каждым тестом
  // restoreMocks: false,

  // Корневой каталог, в котором Jest ищет тесты и модули
  // rootDir: undefined,

  // Каталоги, в которых Jest должен искать файлы
  // roots: [
  //   "<rootDir>"
  // ],

  // Пользовательский runner вместо стандартного runner Jest
  // runner: "jest-runner",

  // Модули, выполняемые для настройки окружения перед запуском тестов
  // setupFiles: [],

  // Модули, выполняемые после установки тестового окружения и перед запуском тестов
  // setupFilesAfterEnv: [],

  // Количество секунд, после которого тест считается медленным
  // slowTestThreshold: 5,

  // Модули для пользовательской сериализации snapshot-тестов
  // snapshotSerializers: [],

  // Окружение, в котором будут выполняться тесты
  // testEnvironment: "jest-environment-node",

  // Настройки, передаваемые тестовому окружению
  // testEnvironmentOptions: {},

  // Добавлять информацию о расположении теста в результаты
  // testLocationInResults: false,

  // Glob-шаблоны, по которым Jest определяет тестовые файлы
  // testMatch: [
  //   "**/__tests__/**/*.?([mc])[jt]s?(x)",
  //   "**/?(*.)+(spec|test).?([mc])[jt]s?(x)"
  // ],

  // Пути к тестам, которые Jest должен игнорировать
  // testPathIgnorePatterns: [
  //   "/node_modules/"
  // ],

  // Регулярное выражение или массив выражений для поиска тестовых файлов
  // testRegex: [],

  // Пользовательский обработчик результатов тестирования
  // testResultsProcessor: undefined,

  // Пользовательский runner для выполнения тестов
  // testRunner: "jest-circus/runner",

  // Сопоставление регулярных выражений с трансформерами файлов
  // transform: undefined,

  // Файлы, которые не должны подвергаться трансформации
  // transformIgnorePatterns: [
  //   "/node_modules/",
  //   "\\.pnp\\.[^\\/]+$"
  // ],

  // Модули, которые не должны автоматически заменяться моками
  // unmockedModulePathPatterns: undefined,

  // Показывать название каждого отдельного теста во время выполнения
  verbose: true,

  // Пути, изменения в которых Jest должен игнорировать в watch-режиме
  // watchPathIgnorePatterns: [],

  // Использовать Watchman для отслеживания файлов
  // watchman: true,
};

export default config;
